{
"dmesg": \[
{
"description": "Display the full kernel ring buffer",
"command": "dmesg"
},
{
"description": "Show only messages related to networking",
"command": "dmesg | grep -i net"
}
],
"Vast Prep": \[
{
"description": "Capture packets on interface eth0",
"command": "tcpdump -i eth0 -nn -vvv -s 0 -w file.pcap"
},
{
"description": "bash script for n in loop",
"command": "for n in {1..100}; do touch 'file\_\$n'; done"
},
{
"description": "ssh keygen command for rsa keypair 4096",
"command": "ssh-keygen -t rsa -b 4096 -f \~/.ssh/kali\_key -N ''"
},
{
"description": "remove all files in directory",
"command": "find . -type f -name "\*" | xargs rm -f"
},
{
"description": "custom PS command to show syscalls",
"command": "ps -eo pid,wchan:32,state,comm | grep 'D'"
},
{
"description": "ping do not fragment",
"command": "ping -M do -s 2000 <serverip>"
},
{
"description": "grep sort command for varlogmessages",
"command": "grep -EI 'error|warning|critical|alert|emerg' /var/log/messages | awk '{print \\\$6, \\\$7, \\\$8, \\\$9}' | sort | uniq -c | sort -nr"
}
],
"awk": \[
{
"description": "Print the second column from input",
"command": "awk '{print \$2}'"
}
]
}

