{
  "dmesg": [
    {
      "description": "Display the full kernel ring buffer",
      "command": "dmesg"
    },
    {
      "description": "Show only messages related to networking",
      "command": "dmesg | grep -i net"
    }
  ],
  "tcpdump": [
    {
      "description": "Capture packets on interface eth0",
      "command": "tcpdump -i eth0"
    }
  ],
  "awk": [
    {
      "description": "Print the second column from input",
      "command": "awk '{print $2}'"
    }
  ]
}

